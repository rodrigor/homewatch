#!/bin/bash
# newsletter_archive.sh — DESATIVADO em 2026-08-23.
#
# Dependia de ~/vault-home, que deixou de existir no Pi (o vault não é mais repo
# git e a cópia local envelhecia sem sincronizar).
#
# Substituto: salve o arquivo em ~/dropped — o Rodrigo traz pro Mac com dropped-pull
# e dá o destino final lá. Para NOTAS, use anota.sh (repo rodrigor/anotacoes).
echo "newsletter_archive.sh: desativado — o vault saiu do Pi. Salve em ~/dropped (vem pelo dropped-pull) ou use anota.sh."
exit 0
